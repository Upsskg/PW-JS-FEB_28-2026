📑 **Agenda for Week 7 Day 1: Jira Integration & OOPs in TypeScript
🎯 [Focus: Mastering Object-Oriented Programming Concepts in TypeScript]

📆 [11th April Sunday]

🕝 Session Highlights

⿡ Recap
Quick review of previous learnings.

1. Jira Integration
Linking test results with Jira issues.
Automating status updates and reporting.

Breakout session for JIRA Bug Creation

2. Class & Objects

Understanding the basics of classes and objects in TypeScript.
Examples: Creating and using classes and objects.

3. Constructors

Role of constructors in initializing objects.
Examples: Using parameterized and default constructors.


4. Access Modifiers

Understanding public, private, and protected access modifiers.
Examples: Controlling access to class properties and methods.

Breakout Session on Access Modifiers

🕡 Wrap Up / Q&A

📝 Keep your physical notebook and pen handy to sketch out ideas and test workflows!

-----------------------------------------------------------------------------------------------

Notes :

Jira :
-----


1. Manuallly Create a bug in JIRA
2. Creating a bug using POSTMAN

1. Endpoint :

https://manual-testing-demoproject.atlassian.net => your domain

 /rest/api/2/issue => specific modules to create an issue

Authorization

2. basic auth :

username : jira account id
password : token


click on profile -> account settings -> secuirity tab -> API token


3.  Creating a bug using Playwright with API





1. Axios → auth is a BUILT-IN FEATURE

In Axios:

auth: {
  username: "email",
  password: "token"
}

👉 Axios automatically converts this into:

Authorization: Basic base64(username:password)

So internally, Axios is doing:

headers: {
  Authorization: "Basic xxxxxxxxx"
}
✅ Equivalent Axios code (manual way)

You could ALSO write:

headers: {
  "Authorization": "Basic " + Buffer.from("email:token").toString("base64")
}

👉 Same result as auth:
👉 But auth: is cleaner

---

## 🔹 What is `testInfo` in Playwright?

`testInfo` is an **object automatically provided by Playwright** that contains **complete runtime information about a test execution**.

👉 Think of `testInfo` as a **report card of the test**.

---

## 🔹 Where does `testInfo` come from?

In hooks like:

```ts
test.afterAll(async ({}, testInfo) => {
  await logADefectInJira(testInfo);
});
```

Playwright **injects `testInfo` for you**.

You **do NOT create it**
You **do NOT import it here**

Playwright does it internally.

---

## 🔹 Why `{}` as the first parameter?

Normally hooks receive **fixtures** first:

```ts
async ({ page }, testInfo) => {}
```

But in `afterAll`:

* You **don’t need page**
* You **only need testInfo**

So you write:

```ts
async ({}, testInfo) => {}
```

👉 `{}` means:

> “I’m ignoring all fixtures”

---

## 🔹 What does `testInfo` contain?

`testInfo` is of type:

```ts
TestInfo
```

(imported in your helper file)

It contains **everything about the test run**.

---

## 🔹 Important `testInfo` Properties (REAL USE)

### ✅ 1️⃣ `testInfo.status`

```ts
testInfo.status
```

Possible values:

* `"passed"`
* `"failed"`
* `"timedOut"`
* `"skipped"`

👉 You used this correctly:

```ts
if (testinfo.status === "failed" || testinfo.status === "timedOut")
```

---

### ✅ 2️⃣ `testInfo.title`

```ts
testInfo.title
```

Returns:

```
Test to record tracing a specific bug - YourName
```

👉 Perfect for **Jira bug summary**

---

### ✅ 3️⃣ `testInfo.error`

testInfo.error
```

Contains:

* Error message
* Stack trace
* Assertion failure

👉 You used it for **Jira description** 💯

---

### ✅ 4️⃣ `testInfo.attachments`

testInfo.attachments

Contains:

* Screenshots
* Videos
* Traces

👉 Can be uploaded to Jira later

---

### ✅ 5️⃣ `testInfo.outputDir`

testInfo.outputDir

Path to:

```
test-results/...
```

Used for **custom reporters**

---

## 🔹 Flow in YOUR Code (End-to-End)

### What actually happens?

1️⃣ Test runs
2️⃣ Test **fails** (Salesforce / locator / timeout)
3️⃣ Playwright records:

* Status
* Error
* Screenshot
* Trace
  4️⃣ `afterAll` hook executes
  5️⃣ Playwright **passes `testInfo`**
  6️⃣ `logADefectInJira(testInfo)` is called
  7️⃣ Jira bug is created using:
* `testInfo.title`
* `testInfo.status`
* `testInfo.error`

👉 **Zero manual intervention**

---

## 🔹 Why `afterAll` is a Smart Choice

| Option      | Result                       |
| ----------- | ---------------------------- |
| `afterEach` | ❌ Creates multiple Jira bugs |
| `afterAll`  | ✅ One clean Jira bug         |
| Inside test | ❌ Pollutes test logic        |
| Reporter    | ✅ Advanced (next level)      |


---

## 🔹 One-Line Definition 

> **`testInfo` is a Playwright-provided runtime object that contains execution status, errors, metadata, and artifacts of a test.**

---



Class and Object:


Real life example of Class and Object, Properties and Methods:

###  What is a *Class*?

A *class* is like a *blueprint* or *template* for creating something.

Think of it like this:
When a car company (like Toyota) designs a new model — say the *Innova* — they first create a *design or blueprint*.
That blueprint has details like:

* The engine type
* Number of seats
* Fuel capacity
* Basic shape and dimensions

But the *blueprint itself is not a car* — it’s just a design or a plan.

Script:
-------




Example 2 with application login :
-----------------------------------

property :
---------
Represents the information or data related to a particular page or object
(e.g., username, password, URL).

method :
-------- 
Represents the actions or operations performed on that page or object
(e.g., fill a field, click a button, submit a form).

******************************************************************************************

Sample Program :
--------------



****************************************************************************************
Constructor:

Definition
A constructor in TypeScript is a method named constructor() that runs once at the time of object creation and sets up the initial state of the object.
Or a Special method to initialize the object

*******************************************************************************************


class LoginPage{

//Properties:
------------

username = "democsr2"
password = "crmsfa"

//Method :

fill(){ // Method block--> block of code to perform an action
//Actionables
fill method
use the properties "username" and "password" to perform the action to fill the login form
}

click(){
//Actionables
click method
}
}


const lp = new LoginPage() // ✔️ Memory is allocated //syntax to create an object(reference/ instance/copy) for a class to call the methods(actionable blocks) inside the class 
lp.fill()
lp.click()

class WelcomePage{
click(){
//Actionables
click method
}
}




function LoginPage(){ // function block
//Actionables
fill method
click method

}

LoginPage()

---------------------------------------------------------------------
